# Cloudflare Pages redirects.
#
# The bare root is NOT handled here: functions/index.js owns it, so it can read
# Accept-Language and send each visitor to the language they actually read
# (falling back to French). A static rule here would shadow that function.
#
# Legacy/typo paths only.
/index.html  /  301
/home        /  301
